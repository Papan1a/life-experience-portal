# Caddyfile
# Reverse proxy with automatic TLS.
# Place your domain below; Caddy obtains Let's Encrypt certs automatically.

{$DOMAIN:localhost} {
    # Spring Boot app
    reverse_proxy app:8080

    # Security headers
    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        X-XSS-Protection "1; mode=block"
        Referrer-Policy strict-origin-when-cross-origin
    }
}

# Adminer — restrict by IP allowlist (VPS internal or VPN).
# Uncomment and set YOUR_VPN_IP / YOUR_TRUSTED_IP for production.
# :8081 {
#     @allowed remote_ip YOUR_VPN_IP YOUR_TRUSTED_IP
#     handle @allowed {
#         reverse_proxy adminer:8080
#     }
#     handle {
#         abort
#     }
# }
