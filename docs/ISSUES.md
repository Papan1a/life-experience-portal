# Issues — проблемы для обсуждения

Сюда записывай проблемы которые нашёл. Формат свободный — просто добавляй пункты списком.
Когда обсудим и решим — переносим в BACKLOG.md или закрываем.

---

## Открытые

<!-- Добавляй сюда -->

~~2. Нет возможности добавления активностей для пользователей и для админа~~ → Backlog #16
1. Для активностей и вариантов есть кнопки редактировать, но нет возможности удалить созданные пользователем

2. у активностей сложность стоимость

~~3. Сделать логин по email регистронезависимым~~ → Backlog #10 (выполнено 27.05.2026)

4. когда активностей становится больше 20, то на главном экране появляется кнопка "Показать ещё", но по нажатию на неё ничего не происходит - кнопки быть не должно вообще, а карточки должны подгружаться бесконечно (пока не подгрузятся все) и просто должна увеличиваться полоса прокрутки. (активностей не такое большое количество, чтобы делать для них отдельные страницы или выводить по частям)

5. Каталог активностей

6. Добавить поиск активностей

~~8. На вкладке "Мой опыт" после Активности указывается код активности, а не её название
в случае с вариантом активности - указывается код активности и надпись Вариант, а должно быть название варианта~~ → Backlog #11 (выполнено 27.05.2026)

9. В описании активности кнопки "интересно", "хочу попробовать" и т.д. находятся не на одной высоте с кнопкой "Сохранить" ("сохранить" располагается выше - нужно выровнять)

~~10. В Профиль - если не загружен аватар, то надо отображать какую-то картинку-заглушку~~ → Backlog #13 (выполнено 27.05.2026)

~~11.a. Убрать "био не заполнено"~~ → Backlog #14 (выполнено 27.05.2026)

~~11.b. Надо отображать зарегистрированный email для входа~~ → Backlog #14 (выполнено 27.05.2026)

12. Кнопки управления в Профиле - надо добавить возможность поменять ФИО, изменить пароль, поменять привязанный email, поменять фотографию.

13. Система жалоб (reports) не работает — жалобы не сохраняются в БД и их нельзя посмотреть в админке.
    **В чём проблема:** сейчас [`ReportController`](src/main/java/com/lep/portal/admin/ReportController.java:35) только пишет `log.warn(...)` в лог-файл, в БД ничего не попадает. В админке [`GET /admin/reports`](src/main/java/com/lep/portal/admin/AdminController.java:105) — заглушка с текстом «Функция отчётов будет доступна в следующей версии». Единственный способ увидеть жалобу — лезть в логи Docker-контейнера.
    **Что нужно сделать:**
    - Создать миграцию с таблицей `reports` (`id`, `reporter_user_id`, `target_type`, `target_id`, `reason`, `comment`, `status`, `created_at`, `resolved_at`, `resolved_by`)
    - Создать entity `Report.java` и `ReportRepository.java`
    - Доработать `ReportController.java` — сохранять жалобу в БД
    - Создать шаблон `admin/reports.html` со списком жалоб, фильтрацией по статусу, возможностью отметить "обработано"
    - Обновить `AdminController.java` (`GET /admin/reports`) — загружать реальные данные из репозитория

15. у многих текстовых полей на формах есть возможность изменять их размеры (например на форме "Сообщить о проблеме" - текстовое поле с описанием проблемы) - нужно запретить любые изменения размеров, положения любых контролов для пользователей!

16. поменять дизайн страницы логирования на что-то подходящее по теме соответствующее общей стилистике портала

17. расположение на странице на вкладке профиль (всё прижато влево без отступов, должен быть отступ как на главной странице с активностями до основного блока)

18. В проекте два шаблона профиля (зачем?):
user/profile.html — используется UserProfileController.viewProfile() (маршрут /users/{id})
profile.html — используется UserController.profile() (маршрут /profile)
должено быть один 

19. Доработка. В настройках профиля надо сделать возможность выбрать Картинку аватара из предложенных. Она должна быть закреплена за пользователем и потом отображаться в разных местах на странице (в топ-бар, в окошке "онлайн")

~~20. в дизайне после того как будет реализован шаблон с тремя столбцами на главной странице, нужно будет перенести в "Каталоге активностей" модуль "Что делают другие" в правый (третий) столбец и ограничить его размер и показывать там только последние 5 событий и иметь кнопку для перехода на новую отдельную форму "Что делают другие", где уже будет вся информация.~~ → Backlog #20 (выполнено 28.05.2026)

21. При установке тегов "Интересно", "Хочу попробовать", "Пробовал(а)", "Хочу повторить" есть кнопка "Снять отметку". Её быть не должно. Должна быть возможность снять отметку повторным кликом на выделенную метку.

22. Тег "Сохранить/Сохранено", и страница "Сохраненное" - надо заменить названия на "Избранное"

23. Подумать над такой темой:
сейчас - есть активности, у них есть Категория
рассмотреть вариант замены категории на теги

24. localhost/activities/new, когда начинаешь вводить "Теги (через запятую)" - в процессе ввода появляется ошибка "⚠️ Запрос не выполнен (400)."

25. Топ-бар:
    - Сделать шире на 35%, чтобы логотип был крупнее и читаемее.
    - Поле поиска центрировать по вертикали (по высоте топ-бара).
    - По горизонтали левый край поля поиска должен начинаться от точки начала 2-го столбца — там, где располагается блок "Каталог активностей".

26. Каталог активностей (2-й столбец) — раскладка категорий:
    - Сейчас на главной вкладке категории размещены в три строки, хотя при текущей ширине экрана места хватает на две.
    - Нужно изменить алгоритм раскладки, чтобы всегда стремиться к минимальному количеству строк. Возможно, достаточно поменять порядок категорий.

27. Карточки активностей — собственные картинки и редизайн макета:
    - Сейчас картинка подтягивается из категории/группы — нужна возможность задавать собственную картинку для каждой карточки. То же касается карточек вариантов активности.
    - Отдельная подзадача — общий макет карточки для главной страницы:
      - слева иконка (загружается при создании; алгоритм генерации обсудим отдельно — пока просто подгрузка с диска);
      - правее в строке — название карточки жирным шрифтом;
      - ниже — короткое описание обычным шрифтом меньшего размера;
      - ещё ниже — теги;
      - в правом верхнем углу — звёздочка отметки "избранное";
      - самой нижней строкой — стоимость, затраченное время, сложность (иконками).

28. Карточка активности целиком кликабельна:
    - Сейчас переход по ссылке срабатывает только при клике на название внутри карточки.
    - Должно срабатывать при клике на любую область карточки — сама карточка должна быть ссылкой/кнопкой.

29. мелкие замечания по дизайну, на странице активностей когда появляется полоса прокрутки справа экрана - это сбивает разметку столбцов, из-за этого возникает неприятный момент дерганья кнтролов и разметки - нужно зафиксировать разметку, либо изначально добавить место под полосу прокрутки, чтобы она не аффектила отображение столбцов 

30. на странице активностей, когда прокручиваешь активности вниз (2й столбец) то в самой начале прокрутки почему-то есть небольшая прокрутка (на несколько пикселей) столбцов 1 и 3 - они должны быть полностью статичными 

31. при переключении между страницей "каталог активности" (главная страница) и страницами "сохраненное" или "мой опыт" и др. сбивается положения столбцов 1, 2, 3 друг относительно друга, на разных страницах у них разная ширина. 
Должно быть всегда одно и то же значение, одинаковое для всех страниц. Там где не используется 3й столбец, он должен слепляться со 2м по толщине, например, для "Сохраненное" столбец 1 - аналогичен главной странице, столбец 2 - занимает ширину столбца 2 с главной + ширину столбца 3. И в нём распологаются сохраненные карточики активностей и карточки вариаций.    

## Обсуждённые / закрытые

<!-- Сюда переносим после обсуждения -->
~~14. На главной верхнее меню, где LEP и кнопки - должно быть закреплено сверху и при прокрутке вниз оставаться на экране всегда~~ → Backlog #14 (выполнено 28.05.2026)

~~1. по одному приглашению можно регистрировать сколько угодно пользователей - должна быть регистрация только одного пользователя, после использования должен меняться статус приглашения на "использован". Так же у приглашения должны быть статусы "Истекший", "Активен", "Отозван"~~

~~4. Активность - кнопки "Интересно", "Хочу попробовать", "Пробовал(а)", "хочу повторить", "Сохранить", "отправить" (в меню сообщить о проблеме) - неактивны, не кликабельны.~~

~~7. Приглашения - ограничить количество приглашений от одного пользователя (одновременно не больше 5)~~
